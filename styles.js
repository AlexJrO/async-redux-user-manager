import { StyleSheet } from "react-native";

export default StyleSheet.create({

  container: {
    flex: 1,
    padding: 20,
    marginTop: 50,
  },

  card: {
    padding: 15,
    marginBottom: 10,
    backgroundColor: "#eee",
  },

  error: {
    color: "red",
    marginBottom: 10,
  },
});